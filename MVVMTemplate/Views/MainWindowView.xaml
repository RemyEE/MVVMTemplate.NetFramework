<Window x:Class="MVVMTemplate.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vml="clr-namespace:MVVMTemplate.VML"
        vml:ViewModelLocator.AutoHookedUpViewModel="True"
        xmlns:local="clr-namespace:MVVMTemplate.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel VerticalAlignment="Center">
        <Label Content="{Binding TheModel.MyString}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Label Content="{Binding TheModel.MyNumber}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button Command="{Binding CountCommand}" Width="100" Height="50" Margin="10">Click me!</Button>
    </StackPanel>
</Window>
